/*
Array Destructuring Exercise:
1. Your task is use array destructuring to extract the first three colors from the colors array and assign them to separate variables named color1, color2, and color3.

2. After extracting the colors, log each variables value to the console.
*/

const colors = ['red', 'green', 'blue', 'yellow', 'orange'];
const [color1, color2, color3] = colors;
console.log(color1, color2, color3);